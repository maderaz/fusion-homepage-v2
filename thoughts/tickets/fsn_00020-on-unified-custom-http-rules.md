# One unified http rules for AMplify

- Files:
  - csp/dev-customHttp.yml
  - csp/mainnet-customHttp.yml
- I want to have only one file instead of two for separate envirnoments
- Identify all files that needs to bechanges
- Run tests
