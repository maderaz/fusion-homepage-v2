# Demo branch — real @ipor_io tweet embed (viewing only)

This branch is **not for merge**. `main` is the clean, homepage-only build for
the dev handoff.

It preserves the pre-strip state (Ecosystem News system + build-time X
syndication tweet embed) so the real tweet can be seen rendering with the real
@ipor_io avatar and live like / reply / repost counts on a Vercel preview.

Look at: `/news/bitgo-institutional-access-tesseract-dedicated-client-vaults`
→ scroll to the embedded tweet (status 1978070150818521291).
